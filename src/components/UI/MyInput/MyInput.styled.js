import styled from 'styled-components';

export const Input = styled.input`
  margin-top: 10px;

  width: 100%;

  font-size: 16px;
  text-align: center;
  line-height: 1.5;

  color: rgba(0, 0, 0, 0.5);

  border: none;
  border-bottom: 1px solid hsla(0, 8%, 40%, 0.4);

  outline: none;
`;
